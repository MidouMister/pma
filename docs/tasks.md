# PMA — Task Tracker (Fresh Start)

**Version:** 3.1.0
**Last Updated:** March 2026
**PRD Reference:** `docs/PRD.md` v2.0.0
**Conventions:** `AGENTS.md` v5.0.0
**Tech Stack:** Next.js 16, React 19, Tailwind CSS 4, shadcn/ui v4, Prisma 7, Clerk, Supabase

> **Legend:** `[ ]` Todo · `[/]` In Progress · `[x]` Done
>
> **Rules before marking `[x]`:**
>
> 1. All unit + integration tests pass (`pnpm test`)
> 2. Linting + typecheck pass (`pnpm lint && pnpm typecheck`)
> 3. Reviewer agent has produced a validation report
> 4. If UI was touched → Playwright screenshot or Devtools MCP confirmation attached

---

## Dependency Graph

```
M00 (Foundation) ──► M01 (Prisma) ──► M02 (Clerk Auth) ──► M03 (Layout)
                                                                  │
                                                                  ▼
M04 (Onboarding) ──► M05 (Company) ──► M06 (Billing) ──► M07 (Team)
                                                               │
                                    ┌──────────────────────────┘
                                    ▼
                               M08 (Clients) ──► M09 (Projects)
                                                      │
                                         ┌────────────┼────────────┐
                                         ▼            ▼            ▼
                                    M10 (Gantt)  M11 (Prod)  M12 (Kanban)
                                                      │            │
                                                      ▼            ▼
                                                 M13 (Comments) M14 (Time)
                                                      │
                                         ┌────────────┘
                                         ▼
                                    M15 (Notifs) ──► M16 (Docs) ──► M17 (Logs)
                                                          │
                                              ┌───────────┘
                                              ▼
                                    M18 (User WS) ──► M19 (Unit Dashboard)
                                                               │
                                                               ▼
                                                   M20 (Landing) ──► M21 (Polish) ──► M22 (Cache)
```

---

## M00 — Project Setup & Dependencies

### Next.js & Core Framework

- [ ] Initialize Next.js 16 with `--turbopack` flag
- [ ] Confirm React 19 is installed
- [ ] Configure `tsconfig.json` with strict mode and path aliases (`@/*` → `./  *`)
- [ ] Configure Tailwind CSS 4 — `tailwind.config.ts` with oklch color tokens
- [x] Initialize shadcn/ui v4 with preset theme + Next.js template — already run:
  ```bash
  pnpm dlx shadcn@latest init --preset b5dfmVWzQ --template next
  ```
  > ⚠️ Do NOT re-run `shadcn init`. Theme, `globals.css`, `cn()`, `tailwind.config.ts`, and `components.json` are already generated by this command. Treat them as the source of truth — never overwrite.
- [ ] Verify `app/globals.css` contains the oklch color token set from the preset (inspect before adding any custom tokens)
- [ ] Add `theme-provider` component (next-themes) if not already scaffolded by the template
- [ ] Verify `lib/utils.ts` has `cn()` — extend with PMA utilities in the SSOT step below

### Install Core Dependencies

- [ ] Install **Clerk**: `@clerk/nextjs`
- [ ] Install **Prisma 7**: `prisma`, `@prisma/adapter-pg`, `pg`
- [ ] Install **Supabase**: `@supabase/supabase-js`, `@supabase/ssr`
- [ ] Install **Jotai**: `jotai`
- [ ] Install **Forms**: `react-hook-form`, `@hookform/resolvers`, `zod`
- [ ] Install **Drag & Drop**: `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities`
- [ ] Install **Date handling**: `date-fns`, `react-day-picker`
- [ ] Install **File uploads**: `uploadthing`, `@uploadthing/react`
- [ ] Install **Utilities**: `sonner`, `lucide-react`, `next-themes`
- [ ] Install **Dev deps**: `dotenv`, `tsx`, `vitest`, `@playwright/test`

### Environment & Configuration

- [ ] Create `.env.local` template with all required keys:
  - `DATABASE_URL`, `DATABASE_URL_TEST`
  - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`
  - `CLERK_WEBHOOK_SECRET`
  - `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
  - `UPLOADTHING_SECRET`, `UPLOADTHING_APP_ID`
- [ ] Create `prisma.config.ts` with Prisma 7 configuration
- [ ] Configure `vitest.config.ts` for unit/integration tests
- [ ] Configure `playwright.config.ts` for E2E tests
- [ ] Configure Husky + lint-staged (pre-commit: lint+format; pre-push: typecheck+test)

### SSOT Library Files

- [ ] Create `lib/types.ts` — all TypeScript interfaces, enums (`Role`, `ProjectStatus`, `PhaseStatus`, `SubPhaseStatus`, `InvitationStatus`, `NotificationType`, `SubscriptionStatus`)
- [ ] Create `lib/cache.ts` — cache tag constants (`CacheTags`) and `cacheLife()` profiles (`static`, `days`, `hours`)
- [ ] Create `lib/validators.ts` — Zod schemas for all entities (see per-milestone validator tasks)
- [ ] Create `lib/utils.ts` — extend with:
  - `formatAmount(n)` → `"1 234 567,89 DA"` (Algerian format)
  - `formatDate(d)` → `"DD/MM/YYYY"`
  - `formatDelai(months, days)` → `"X mois Y jours"`
  - `calculateTVA(ht, ttc)` → `{ amount, percentage }`
  - `calculateProgress(phases)` → weighted average
- [ ] Create `lib/db.ts` — Prisma singleton with `@prisma/adapter-pg`
- [ ] Create `lib/supabase.ts` — Supabase browser client
- [ ] Create `lib/supabase-server.ts` — Supabase server client (cookie-based)
- [ ] Create `lib/permissions.ts` — RBAC helper functions (e.g. `canEditProject()`, `canManageUnit()`)

### Jotai Store & Hooks

- [ ] Create `store/theme.ts` — dark/light mode atom (persisted to localStorage)
  > ⚠️ No `sidebar.ts` atom needed — `sidebar-07` manages open/collapsed state internally via `useSidebar()` hook and a cookie. Do NOT add a competing Jotai atom for sidebar state.
- [ ] Create `store/index.ts` — barrel export
- [ ] Create `hooks/useTimer.ts` — live timer hook (start/stop/reset, returns `hh:mm:ss`)
- [ ] Create `hooks/useNotifications.ts` — polling hook (30s interval, calls `getUnreadCount()`)
- [ ] Create `hooks/index.ts` — barrel export

### shadcn/ui Components

- [ ] Install all required components via `pnpm dlx shadcn@latest add`:
  - `button`, `input`, `textarea`, `select`, `checkbox`, `radio-group`, `switch`
  - `dialog`, `sheet`, `alert-dialog`, `popover`, `dropdown-menu`, `command`
  - `card`, `table`, `tabs`, `badge`, `progress`, `separator`, `scroll-area`
  - `avatar`, `skeleton`, `tooltip`, `sonner`
  - `form` (react-hook-form integration)
  - `sidebar` — **required primitive** for the `sidebar-07` block (installs `SidebarProvider`, `useSidebar`, all sub-components)
    > Note: `sidebar-07` block (full scaffolded layout) is installed in **M03**, not here. This step only installs the primitive.

### Root Layout

- [ ] Create `app/layout.tsx`:
  - `lang="fr"`, DM Sans font (Google Fonts via `next/font`)
  - `JotaiProvider` wrapper
  - `ThemeProvider` (next-themes, attribute="class")
  - `Toaster` (sonner)
  - _(ClerkProvider added in M02)_

### Tests

- [ ] Unit test: `formatAmount()` covers edge cases (0, large numbers, negative)
- [ ] Unit test: `formatDelai()` covers 0-months, 0-days combinations
- [ ] Unit test: `calculateProgress()` with weighted phases

---

## M01 — Prisma Schema & Database

> Schema path: `prisma/schema.prisma` · Output: `generated/prisma/`

### Schema Generator Block

- [ ] Add Prisma 7 generator block:
  ```prisma
  generator client {
    provider = "prisma-client"
    output   = "../generated/prisma"
  }
  datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
  }
  ```

### Enums

- [ ] Define `Role`: `OWNER | ADMIN | USER`
- [ ] Define `ProjectStatus`: `New | InProgress | Pause | Complete`
- [ ] Define `PhaseStatus`: `New | InProgress | Pause | Complete`
- [ ] Define `SubPhaseStatus`: `New | InProgress | Pause | Complete`
- [ ] Define `InvitationStatus`: `PENDING | ACCEPTED | REJECTED | EXPIRED`
- [ ] Define `NotificationType`: `INVITATION | PROJECT | TASK | TEAM | PHASE | CLIENT | PRODUCTION | LANE | TAG | GENERAL`
- [ ] Define `SubscriptionStatus`: `TRIAL | ACTIVE | GRACE | READONLY | SUSPENDED`

### Core Models

- [ ] `Plan` — `id, name, maxUnits(Int?), maxProjects(Int?), maxTasksPerProject(Int?), maxMembers(Int?), priceDA(Int)`
  - `null` on any max field = unlimited (BR-06)
- [ ] `Company` — `id, name, ownerId(unique), logo?, NIF(unique), RC(unique), NIS(unique), AI(unique), formJur, sector, wilaya, address, phone, email, productionAlertThreshold(default:80)`
- [ ] `Subscription` — `id, companyId(unique), planId, status(SubscriptionStatus), startAt, endAt`
- [ ] `User` — `id, clerkId(unique), name, email(unique), role(Role), companyId, unitId?`
  - `unitId` is `null` for OWNER (BR-04a)
- [ ] `Unit` — `id, companyId, adminId?(unique within company), name, address?, phone?, email?, logo?`
- [ ] `Invitation` — `id, companyId, unitId, email, role(Role), token(unique), status(InvitationStatus), expiresAt`
  - Unique constraint: `[unitId, email]`

### Operational Models

- [ ] `Client` — `id, unitId, companyId, name, wilaya?, phone?, email?`
  - Unique: `[unitId, name]`, `[unitId, email]`
- [ ] `Project` — `id, unitId, companyId, clientId?, name, code, type?, montantHT, montantTTC, ods(DateTime), delaiMonths(default:0), delaiDays(default:0), status(ProjectStatus,default:New), signe(Boolean,default:false), progress(default:0)`
  - Unique: `[unitId, code]`
- [ ] `Team` — `id, projectId(unique)`
- [ ] `TeamMember` — `id, teamId, userId, roleLabel?`
  - Unique: `[teamId, userId]`
- [ ] `Phase` — `id, projectId, name, code, montantHT, startDate, endDate, duration(auto), status(PhaseStatus,default:New), progress(default:0), observations?`
- [ ] `SubPhase` — `id, phaseId, name, code?, status(SubPhaseStatus,default:New), progress(default:0), startDate, endDate`
- [ ] `GanttMarker` — `id, projectId, label, date, className?`
- [ ] `Product` — `id, phaseId(unique), taux(default:0), montantProd(default:0), date?`
- [ ] `Production` — `id, productId, phaseId, taux, mntProd(system-calculated), date`

### Execution & Tracking Models

- [ ] `Lane` — `id, unitId, companyId, name, color, order(Int)`
- [ ] `Tag` — `id, unitId, companyId, name, color`
- [ ] `Task` — `id, unitId, companyId, projectId, phaseId?, subPhaseId?, laneId, assignedUserId?, title, description?, startDate?, dueDate?, endDate?, complete(default:false), order(Int)`
- [ ] `TaskTag` — junction: `taskId, tagId` (unique `[taskId, tagId]`)
- [ ] `TaskComment` — `id, taskId, authorId, companyId, body, edited(default:false), createdAt`
- [ ] `TaskMention` — `id, commentId, mentionedUserId, companyId`
  - Unique: `[commentId, mentionedUserId]`
- [ ] `TimeEntry` — `id, companyId, userId, projectId, taskId?, description?, startTime, endTime, duration(Int, minutes)`
- [ ] `Notification` — `id, companyId, unitId?, userId, type(NotificationType), message, read(default:false), targetRole?, targetUserId?`
- [ ] `ActivityLog` — `id, companyId, unitId?, userId, action, entityType, entityId, metadata(Json?), createdAt`

### Indexes

- [ ] Add `@@index` on all foreign key fields across every model for query performance
- [ ] Add `@@index([companyId])` on every model that has `companyId` (tenant isolation)

### Database Operations

- [ ] Generate Prisma client: `pnpm exec prisma generate`
- [ ] Create initial migration: `pnpm exec prisma migrate dev --name init`
- [ ] Create `prisma/seed.ts` — seed 3 Plan recor ds:
  - **Starter**: `maxUnits=1, maxProjects=5, maxTasksPerProject=20, maxMembers=10, priceDA=0`
  - **Pro**: `maxUnits=5, maxProjects=30, maxTasksPerProject=200, maxMembers=50`
  - **Premium**: all max fields `null` (unlimited)
- [ ] Run seed: `pnpm exec prisma db seed`

### Tests

- [ ] Verify all relations resolve correctly (no orphaned foreign keys)
- [ ] Verify unique constraints are enforced (NIF, RC, NIS, AI, `[unitId, code]`, etc.)

---

## M02 — Authentication (Clerk)

### Environment

- [ ] Add `ClerkProvider` to `app/layout.tsx` wrapping all children
- [ ] Configure `.env.local` with Clerk publishable key + secret key

### Middleware

- [ ] Create `middleware.ts` (Clerk `clerkMiddleware`):
  - Public routes: `/`, `/company/sign-in/(.*)`, `/company/sign-up/(.*)`
  - All other routes: protected (redirect to sign-in if unauthenticated)
- [ ] Role-based redirect logic after auth:
  - `OWNER` → `/company/[companyId]`
  - `ADMIN` → `/unite/[unitId]`
  - `USER` → `/dashboard`
  - No company (new user) → `/onboarding`
- [ ] Read `role` and `companyId` from Clerk `publicMetadata` for redirect decisions

### Auth Pages

- [ ] Create `app/(auth)/company/sign-in/[[...sign-in]]/page.tsx`
  - Use `<SignIn>` Clerk component
  - "Refined Professional" aesthetic — dark gradient, logo, tagline
- [ ] Create `app/(auth)/company/sign-up/[[...sign-up]]/page.tsx`
  - Use `<SignUp>` Clerk component
  - Same aesthetic as sign-in

### Webhooks

- [ ] Create `app/api/webhooks/clerk/route.ts`:
  - Verify `svix` signature
  - `user.created` → insert `User` record, set Clerk `publicMetadata.role = null` (pre-onboarding)
  - `user.updated` → update `User.name` and `User.email`
  - `user.deleted` → delete `User` from DB
- [ ] Add `CLERK_WEBHOOK_SECRET` to env

### Invitation Accept Flow

- [ ] Create `InvitationProcessor` client component:
  - Reads invitation token from URL params after sign-up
  - Calls `acceptInvitation(token)` server action
  - Redirects to correct dashboard on success
- [ ] Mount `InvitationProcessor` in sign-up success redirect or onboarding check

### Tests

- [ ] Unit test: middleware redirect logic (mock `publicMetadata` for each role)
- [ ] Unit test: webhook handler — `user.created` creates DB record
- [ ] Unit test: webhook handler — `user.deleted` removes DB record

---

## M03 — Root Layout & Global Components

### Dashboard Layout

- [ ] Scaffold `sidebar-07` block into the project:
  ```bash
  pnpm dlx shadcn@latest add sidebar-07
  ```
  > This generates a full dashboard shell: `app/(dashboard)/layout.tsx`, `components/app-sidebar.tsx`, and wires `SidebarProvider` + `SidebarInset`. Use this output as the base — do not build layout from scratch.
- [ ] Review all generated files before modifying — understand the `SidebarProvider` / `SidebarInset` / `useSidebar()` structure
- [ ] Update `app/(dashboard)/layout.tsx`:
  - Keep `SidebarProvider` as the outermost wrapper (it manages open state + cookie persistence)
  - Add server-side fetch for current user + subscription status — pass as props into `<AppSidebar>`
  - Render `<AppSidebar>` + `<SidebarInset>` (which contains `<Navbar>` + `{children}`)

### Sidebar Component (`components/app-sidebar.tsx`)

> Base: generated by `sidebar-07`. Customize — do not rewrite from scratch.

- [ ] Replace placeholder `navMain` / `navSecondary` data with PMA's role-based nav config:
  - Define a `getNavItems(role, companyId, unitId)` helper in `lib/nav.ts` that returns the correct item groups per role:
    - **OWNER:** Company Dashboard, Units, Company Team, Billing, _(divider)_, Unit Dashboard, Projects, Kanban, Clients, Unit Members, Unit Settings, _(divider)_, Notifications
    - **ADMIN:** Unit Dashboard, Projects, Kanban, Clients, Unit Members, Unit Settings, _(divider)_, Notifications
    - **USER:** Unit Dashboard, Projects, Kanban, _(divider)_, Notifications
  - Each item: `{ title, url, icon }` using `lucide-react` icons
- [ ] Active route highlighting — use `usePathname()` to compare against `item.url`; apply the sidebar's built-in `isActive` prop on `SidebarMenuButton`
- [ ] User footer (already scaffolded by `sidebar-07`):
  - Replace placeholder with Clerk user data: avatar (`UserButton` or `<Avatar>` with Clerk `imageUrl`), full name, role badge
  - Add trial/subscription countdown chip below the user info (colored: green > 14 days, amber ≤ 14, red ≤ 3)
- [ ] Mobile behavior: `sidebar-07` already handles sheet/drawer on mobile via `SidebarProvider` — verify it works, do not add a competing Sheet
- [ ] Apply any theme overrides (glassmorphism, border, background) via CSS variables in `globals.css` — do NOT modify `components/ui/sidebar.tsx` directly

### Navbar Component (`components/layout/Navbar.tsx`)

> In `sidebar-07`, the header area lives inside `<SidebarInset>` as a `<header>` element. Build `<Navbar>` as a standalone component rendered inside that header slot.

- [ ] `SidebarTrigger` button (already provided by sidebar-07 — keep it as the collapse toggle)
- [ ] Breadcrumbs (dynamic, built from `usePathname()` segments)
- [ ] Global search (Command palette trigger — `⌘K`)
- [ ] `<NotificationBell>` with unread count badge (polling, see M15)
- [ ] `<ThemeToggle>` (dark/light, updates Jotai `themeAtom` + `next-themes`)
- [ ] Glassmorphism styling via CSS variables (do not inline styles)

### InfoBar Component (`components/layout/InfoBar.tsx`)

- [ ] Contextual info strip (shown on Project / Unit pages)
- [ ] Displays: entity name, status badge, key metric

### Dashboard Redirect Hub

- [ ] Create `app/(dashboard)/dashboard/page.tsx`:
  - Server component, reads Clerk `publicMetadata`
  - OWNER → redirect to `/company/[companyId]`
  - ADMIN → redirect to `/unite/[unitId]`
  - USER → redirect to `/user/[userId]`

### Tests

- [ ] Unit test: sidebar renders correct menu items per role
- [ ] E2E: authenticated user lands on correct dashboard after sign-in

---

## M04 — Onboarding Wizard

> **Critical:** `consolidateOnboarding()` must set `publicMetadata.role = OWNER` and `publicMetadata.companyId` in Clerk before returning, so middleware redirects correctly.

### Route

- [ ] Create `app/(onboarding)/onboarding/page.tsx`
- [ ] Guard: if user already has `publicMetadata.companyId`, redirect to their dashboard

### Multi-Step Form Component (`components/onboarding/CompanyOnboardingForm.tsx`)

- [ ] Step indicator (3 steps, visual progress bar)
- [ ] Step 1 — Company Profile:
  - Company name (required)
  - Logo upload via Uploadthing (optional, stores URL)
  - Legal form dropdown: `SARL | EURL | SPA | EIRL | SNC | Autre`
  - Sector dropdown: `Construction | Engineering | PublicWorks | Industry | Autre`
  - Fiscal IDs (all required, unique-validated on blur via server action):
    - NIF (format: `XXXXXXXXXXXXXXXXXX`)
    - RC (Registre de Commerce)
    - NIS (Numéro d'Identification Statistique)
    - AI (Article d'Imposition)
  - Wilaya dropdown (48 Algerian wilayas)
  - Address, phone, email
- [ ] Step 2 — First Unit:
  - Unit name (required)
  - Address, phone, email (optional)
- [ ] Step 3 — Invite Team (optional):
  - Email input + role picker (ADMIN or USER only — never OWNER)
  - Add multiple invites (list with remove)
  - "Skip" button
- [ ] Navigation: Back / Next / Submit

### Validators

- [ ] `companySchema` — Algerian field formats (NIF: 20 digits, RC: alphanumeric, etc.)
- [ ] `unitSchema` — name required, max 100 chars
- [ ] `invitationSchema` — valid email, role must be ADMIN or USER

### Server Actions (`lib/queries/onboarding.ts`)

- [ ] `checkFiscalIdentity(field, value)` — returns `{ taken: boolean }` for blur validation
- [ ] `consolidateOnboarding(data)` — single transaction:
  1. Create `Company` record
  2. Create `Subscription` (Starter plan, `startAt=now`, `endAt=now+2months`, status=TRIAL)
  3. Create first `Unit` with `adminId=null`
  4. Update `User.role = OWNER`, `User.companyId`, `User.unitId = null`
  5. Set Clerk `publicMetadata`: `{ role: "OWNER", companyId, unitId: null }`
  6. Send invitations if provided (create `Invitation` records + Clerk invitation emails)
  7. `revalidateTag(CacheTags.company)`

### Tests

- [ ] Unit test: `consolidateOnboarding()` — company + subscription + unit created in one transaction
- [ ] Unit test: `checkFiscalIdentity()` — returns `taken: true` for duplicate NIF
- [ ] Unit test: duplicate fiscal identity is blocked
- [ ] E2E: full onboarding flow → lands on company dashboard

---

## M05 — Company Management (OWNER only)

### Routes

- [ ] Create `app/(dashboard)/company/[companyId]/page.tsx` — Company Dashboard
- [ ] Create `app/(dashboard)/company/[companyId]/settings/page.tsx` — Company Settings
- [ ] Create `app/(dashboard)/company/[companyId]/units/page.tsx` — Units Management
- [ ] Create `app/(dashboard)/company/[companyId]/team/page.tsx` — Company Team

### Company Dashboard (`/company/[companyId]`)

- [ ] KPI cards (server-fetched):
  - Total projects (all units)
  - Active projects (status = InProgress)
  - Total contract value (sum `montantTTC` all projects)
  - Total members (all units)
  - Production health (% of phases with no variance alerts)
- [ ] Unit overview cards (per unit: name, member count, active project count)

### Company Settings (`/company/[companyId]/settings`)

- [ ] `CompanyForm` — edit: name, logo, address, phone, email, wilaya, sector, legal form
- [ ] Fiscal identity display (NIF, RC, NIS, AI) — read-only after creation
- [ ] `productionAlertThreshold` slider (1–100, default 80) — BR-15
- [ ] Danger zone: Delete Company (confirmation dialog, cascades all data — BR-01)

### Units Management (`/company/[companyId]/units`)

- [ ] Unit cards grid: name, admin name (or "No admin"), member count, project count
- [ ] Create unit button → `UnitModal` (checks `Plan.maxUnits` — BR-05)
- [ ] Edit unit → `UnitModal` prefilled
- [ ] Delete unit → confirmation dialog (cascades: projects, tasks, clients — UNIT-06)

### Company Team (`/company/[companyId]/team`)

- [ ] Table: avatar, name, email, role badge, unit name, joined date
- [ ] Read-only (invitations managed per-unit in M07)
- [ ] Filters: role, unit

### Server Actions (`lib/queries/company.ts`)

- [ ] `getCompanyHeader(companyId)` — `cacheLife("static")`
- [ ] `getCompanyById(companyId)` — `cacheLife("days")`, `cacheTag(CacheTags.company)`
- [ ] `getCompanyDashboardData(companyId)` — KPI aggregates
- [ ] `updateCompany(companyId, data)` — `revalidateTag(CacheTags.company)`
- [ ] `deleteCompany(companyId)` — cascade delete, revoke Clerk session
- [ ] `createUnit(companyId, data)` — check `maxUnits` limit (BR-05), `revalidateTag(CacheTags.units)`
- [ ] `updateUnit(unitId, data)` — `revalidateTag(CacheTags.units)`
- [ ] `deleteUnit(unitId)` — cascade delete, `revalidateTag(CacheTags.units)`
- [ ] `getCompanyTeam(companyId)` — all users across all units

### Validators

- [ ] Extend `companySchema` with `productionAlertThreshold` (Int, 1–100)
- [ ] `unitSchema` — name, address, phone, email

### Tests

- [ ] Unit test: `createUnit()` blocks when `maxUnits` limit reached
- [ ] Unit test: `deleteCompany()` cascades all child records
- [ ] Unit test: `updateCompany()` calls `revalidateTag`

---

## M06 — Subscription & Billing

### Billing Page (`/company/[companyId]/settings/billing`)

- [ ] Create page route
- [ ] Plan info card: plan name, status badge, `startAt`, `endAt`, days remaining
- [ ] Limits usage section — progress bars for each limit:
  - Units used / max
  - Projects used / max
  - Members used / max
- [ ] Trial countdown: highlighted when < 7 days remain
- [ ] Upgrade CTA button (opens `UpgradeRequestForm`)

### Upgrade Request Flow

- [ ] `UpgradeRequestForm` Dialog:
  - Fields: desired plan (Pro / Premium), contact email, phone, payment method (Virement Bancaire / Chèque / Autre), message (optional)
  - Submit → `requestUpgrade()` server action
- [ ] `requestUpgrade(companyId, data)` — creates `GENERAL` notification to OWNER + logs activity

### Subscription Enforcement (used across all mutations)

- [ ] Create `lib/subscription.ts` utility:
  - `getSubscriptionStatus(companyId)` — returns `{ status, daysRemaining, isReadOnly, isGrace }`
  - `checkPlanLimit(companyId, limitKey)` — returns `{ allowed: boolean, current, max }`
  - `enforceSubscription(companyId)` — throws if `READONLY` or `SUSPENDED` (BR-08)
- [ ] `UpgradePromptModal` component — shown when any plan limit is hit
- [ ] Persistent upgrade banner component (shown when status = GRACE)
- [ ] Days-remaining countdown chip (used in Sidebar + Billing page)

### Trial Notifications (computed — no background job needed)

- [ ] On page load, compute days remaining from `Subscription.endAt`
- [ ] Trigger `GENERAL` notification creation if days match: T-30, T-7, T-3 (idempotent — check if already sent)

### Tests

- [ ] Unit test: `checkPlanLimit()` — returns `allowed: false` when at max
- [ ] Unit test: `checkPlanLimit()` — returns `allowed: true` when limit is `null` (BR-06)
- [ ] Unit test: `enforceSubscription()` — throws when status = READONLY
- [ ] Unit test: grace period detection (7 days after `endAt`)

---

## M07 — Team & Invitations

### Server Actions (`lib/queries/team.ts`)

- [ ] `inviteMember(unitId, { email, role })`:
  - Block `Role.OWNER` invitations (BR-04)
  - Check `Plan.maxMembers` (BR-05)
  - Check no duplicate pending invitation for `[unitId, email]`
  - Create `Invitation` DB record (token = `nanoid()`, expires = now + 7 days)
  - Send Clerk invitation email with token in URL
  - `revalidateTag(CacheTags.invitations)`
- [ ] `cancelInvitation(invitationId)` — set status = EXPIRED, `revalidateTag`
- [ ] `resendInvitation(invitationId)` — regenerate token, extend `expiresAt` by 7 days
- [ ] `acceptInvitation(token)`:
  - Validate token exists, status = PENDING, not expired
  - Assign `User.role`, `User.unitId`, `User.companyId`
  - If role = ADMIN: set `Unit.adminId = user.id`
  - Update `Invitation.status = ACCEPTED`
  - Set Clerk `publicMetadata`: `{ role, companyId, unitId }`
  - `revalidateTag(CacheTags.team)`
- [ ] `removeMember(userId)`:
  - Set `User.unitId = null`, `User.role = null` (or mark inactive)
  - If user was unit admin: set `Unit.adminId = null`
  - Retain all tasks and time entries (BR-16)
  - `revalidateTag(CacheTags.team)`

### Member Directory (`/unite/[unitId]/members`)

- [ ] Create page route
- [ ] Members table: avatar, name, email, role badge, status (active/pending), joined date
- [ ] "Invite Member" button → slide-over `Sheet` with invite form
- [ ] Pending invitations section: email, role, expires at, cancel / resend actions
- [ ] Remove member action (confirmation dialog)

### Validators

- [ ] `invitationSchema` — valid email, role ∈ `[ADMIN, USER]` only

### Tests

- [ ] Unit test: `inviteMember()` blocks OWNER role
- [ ] Unit test: `inviteMember()` blocks duplicate pending invitation
- [ ] Unit test: `inviteMember()` blocks when `maxMembers` reached
- [ ] Unit test: `acceptInvitation()` with expired token returns error
- [ ] Unit test: `removeMember()` retains tasks and time entries

---

## M08 — Client CRM

### Server Actions (`lib/queries/clients.ts`)

- [ ] `createClient(unitId, data)` — unique name + email within unit (CLT-03), `revalidateTag(CacheTags.clients)`
- [ ] `updateClient(clientId, data)` — `revalidateTag(CacheTags.clients)`
- [ ] `deleteClient(clientId)` — block if any project with `status = InProgress` (BR-20), `revalidateTag`

### Client List (`/unite/[unitId]/clients`)

- [ ] Create page route
- [ ] Table: name, wilaya, phone, email, project count, total TTC
- [ ] Search by name (client-side filter)
- [ ] Sort: name A-Z / total contract value desc
- [ ] "New Client" button → `ClientSheet` (create mode)
- [ ] Row click → `ClientSheet` (edit mode)

### Client Sheet (`components/clients/ClientSheet.tsx`)

- [ ] Fields: name, wilaya (dropdown), phone, email
- [ ] Linked projects list (read-only in sheet view)
- [ ] Total TTC display
- [ ] Delete button (ADMIN/OWNER only, with active-project guard)

### Validators

- [ ] `clientSchema` — name required, email valid format, wilaya ∈ 48 wilayas

### Tests

- [ ] Unit test: `createClient()` blocks duplicate name within same unit
- [ ] Unit test: `deleteClient()` blocks when active InProgress project exists
- [ ] Unit test: client in Unit A is invisible to Unit B (BR-19)

---

## M09 — Project Management

### Server Actions (`lib/queries/projects.ts`)

- [ ] `createProject(unitId, data)`:
  - Check `Plan.maxProjects` (BR-05)
  - Auto-create `Team` record
  - Create `PROJECT` notification (OWNER + ADMIN)
  - `revalidateTag(CacheTags.projects)`
- [ ] `updateProject(projectId, data)` — `revalidateTag(CacheTags.projects)`
- [ ] `deleteProject(projectId)` — cascade tasks, phases, productions, team; `revalidateTag`
- [ ] `archiveProject(projectId)` — soft-delete (set archived flag or status)
- [ ] `addTeamMember(projectId, userId)` — create `TeamMember`, send `TEAM` notification
- [ ] `removeTeamMember(projectId, userId)` — delete `TeamMember`, send `TEAM` notification

### Project List (`/unite/[unitId]/projects`)

- [ ] Create page route
- [ ] Table / card view: name, code, client name, status badge, `montantTTC`, progress bar
- [ ] Filters: status, client
- [ ] Sort: creation date, `montantTTC`
- [ ] "New Project" button → `ProjectFormModal`
- [ ] USER sees only projects where they are a `TeamMember` (PROJ-08)

### Project Detail (`/unite/[unitId]/projects/[projectId]`)

- [ ] Create page route with tab layout: `Overview | Gantt | Production | Tasks | Time Tracking | Documents`
- [ ] **Overview tab:**
  - Financials: montantHT, montantTTC, TVA amount + %, signe badge
  - Progress: weighted average progress bar (BR-17)
  - ODS date, Délai display (`formatDelai()`)
  - Client card (name, wilaya, phone)
  - Team section: member avatars, add/remove (ADMIN/OWNER only)
  - Status lifecycle buttons (New → InProgress → Pause → Complete)
- [ ] Tab routing: each tab is a URL segment or query param for deep-linking

### ProjectFormModal (`components/projects/ProjectFormModal.tsx`)

- [ ] Fields: name, code (unique within unit), type, montantHT, montantTTC, ODS date, delaiMonths, delaiDays, status, signe toggle, clientId (select)
- [ ] Auto-calculate TVA on `montantTTC` / `montantHT` change
- [ ] Validate: code uniqueness on blur

### Validators

- [ ] `projectSchema` — all fields with correct types, `montantTTC >= montantHT`, `ods` valid date

### Tests

- [ ] Unit test: `createProject()` blocks when `maxProjects` limit reached
- [ ] Unit test: `createProject()` auto-creates Team record
- [ ] Unit test: RBAC — USER cannot call `createProject()`
- [ ] Unit test: `Project.progress` recalculates on phase progress change (BR-17)

---

## M10 — Phase & Gantt Planning

### Setup

- [ ] **M10.0** Install Kibo UI Gantt: `pnpm component:add gantt`
  - _Touches: `package.json`, `components/ui/gantt/`_
  - Depends on: M09

### Server Actions (`lib/queries/phases.ts`)

- [ ] **M10.1** `createPhase(projectId, data)`:
  - Validate `startDate >= Project.ods` (BR-11)
  - Validate `sum(Phase.montantHT) + new <= Project.montantHT` (BR-10) — **block save, return remaining budget in error**
  - Auto-calculate `duration = (endDate - startDate)` in days (PH-04)
  - `revalidateTag(CacheTags.phases)`
- [ ] **M10.2** `updatePhase(phaseId, data)` — same validations as create
- [ ] **M10.3** `deletePhase(phaseId)` — cascade SubPhases, Product, Productions; `revalidateTag`
- [ ] **M10.4** `createSubPhase(phaseId, data)` — validate dates within parent Phase range (BR-12)
- [ ] **M10.5** `updateSubPhase(subPhaseId, data)` — same date validation
- [ ] **M10.6** `deleteSubPhase(subPhaseId)`
- [ ] **M10.7** `reschedulePhase(phaseId, { startDate, endDate })` — called after Gantt drag & drop
- [ ] **M10.8** `createGanttMarker(projectId, data)` / `deleteGanttMarker(markerId)`

### Gantt Component (`components/gantt/GanttView.tsx`)

- [ ] **M10.9** Build `GanttView` using Kibo UI `<Gantt>`:
  - `items` prop: map phases + subphases to Kibo UI schema
  - `grouping` prop: SubPhases nested under their parent Phase row
  - Today marker via `todayMarker` prop
  - GanttMarkers from DB rendered as additional vertical markers
  - Progress fill overlay on each bar (customize bar appearance via CSS)
- [ ] **M10.10** Drag & drop reschedule → call `reschedulePhase()` on drag end
- [ ] **M10.11** Timeline zoom toggle: Month / Week / Day (`timeRange` prop)
- [ ] **M10.12** Phase detail `Sheet` (480px) on bar click:
  - Edit phase: name, code, montantHT, startDate, endDate, status, progress, observations
  - SubPhase list with inline add/edit
- [ ] **M10.13** Overlap warning (visual highlight) using `onItemsOverlap` callback
- [ ] **M10.14** Performance test: 50+ phases render without visible lag (NFR-03)

### Realtime

- [ ] Supabase channel: `project:{projectId}:phases` — revalidate Gantt on remote change

### Tests

- [ ] Unit test: `createPhase()` blocks when budget sum would exceed project `montantHT` (BR-10)
- [ ] Unit test: `createPhase()` blocks when `startDate < Project.ods` (BR-11)
- [ ] Unit test: `createSubPhase()` blocks when dates outside parent Phase range (BR-12)
- [ ] Unit test: `reschedulePhase()` updates dates correctly

---

## M11 — Production Monitoring

### Server Actions (`lib/queries/production.ts`)

- [ ] `createProduct(phaseId, data)` — one per phase; if Product exists, block with error
- [ ] `updateProduct(productId, data)` — update planned `taux` / `montantProd`
- [ ] `createProduction(productId, data)`:
  - System-calculates `mntProd = Phase.montantHT × (taux / 100)` (BR-13)
  - If `taux < Product.taux × Company.productionAlertThreshold / 100`: create `PRODUCTION` notification to OWNER (BR-14)
  - `revalidateTag(CacheTags.production)`
- [ ] `updateProduction(productionId, data)` — recalculate `mntProd` on save
- [ ] `deleteProduction(productionId)` — `revalidateTag`

### Production Tab (inside Project Detail)

- [ ] Line chart: Planned taux vs. Actual taux over time (Recharts or similar)
- [ ] Grouped bar chart: Planned `montantProd` vs. Actual `mntProd` per phase
- [ ] Data table:
  - Columns: date, planned taux (%), actual taux (%), variance (%), `mntProd` (DA)
  - Red row styling when `actual < planned × threshold%`
- [ ] "Record Production" button → modal (select phase, enter taux, date)

### Unit-wide Production Page (`/unite/[unitId]/productions`)

- [ ] Create page route
- [ ] Aggregated production data across all unit projects
- [ ] Filter by project, date range

### Validators

- [ ] `productionSchema` — taux: Int 0–100, date: valid date

### Tests

- [ ] Unit test: `createProduction()` calculates `mntProd` server-side (BR-13)
- [ ] Unit test: `createProduction()` fires PRODUCTION notification when below threshold (BR-14)
- [ ] Unit test: user cannot set `mntProd` directly

---

## M12 — Task & Kanban Board

### Setup

- [ ] **M12.0** Install Kibo UI Kanban: `pnpm component:add kanban`
  - _Touches: `package.json`, `components/ui/kanban/`_
  - Depends on: M09

### Server Actions (`lib/queries/tasks.ts`)

- [ ] **M12.1** Lane CRUD:
  - `createLane(unitId, data)` — send `LANE` notification
  - `updateLane(laneId, data)`
  - `deleteLane(laneId)` — block if tasks exist in lane, or reassign tasks
  - `reorderLanes(unitId, orderedIds)` — update `order` field
- [ ] **M12.2** `createTask(unitId, data)`:
  - Check `Plan.maxTasksPerProject` for the project (BR-05)
  - Send `TASK` notification to `assignedUserId`
  - `revalidateTag(CacheTags.tasks)`
- [ ] **M12.3** `updateTask(taskId, data)` — `revalidateTag(CacheTags.tasks)`
- [ ] **M12.4** `deleteTask(taskId)` — cascade TaskComments, TaskTags, TimeEntries
- [ ] **M12.5** `moveTask(taskId, { laneId, order })` — update lane + reorder; lanes are Unit-scoped (BR-21)
- [ ] **M12.6** Tag CRUD: `createTag()`, `updateTag()`, `deleteTag()`, `assignTagToTask()`, `removeTagFromTask()`

### Kanban Board (`/unite/[unitId]/kanban`)

- [ ] Create page route
- [ ] **M12.7** `KanbanBoard` component using Kibo UI `<Kanban>`:
  - Columns = Lanes (Unit-scoped)
  - Cards = Tasks (card content: title, due date badge, assignee avatar, tag chips)
  - Overdue badge (red) when `dueDate < now && !complete`
- [ ] **M12.8** Drag & drop between columns → `moveTask()` (ADMIN/OWNER: any card; USER: own tasks only — BR-21)
- [ ] **M12.9** Filter bar: Project → Phase → SubPhase (sequential cascading dropdowns)
- [ ] **M12.10** "New Task" button on each lane header
- [ ] **M12.11** Card click → `TaskDetailSheet` (480px slide-over)
- [ ] **M12.12** Performance test: 200 tasks across 10 lanes (NFR-04)

### Task Detail Sheet (`components/tasks/TaskDetailSheet.tsx`)

- [ ] Title (inline editable), description (rich text or textarea)
- [ ] Lane selector (dropdown)
- [ ] Project + Phase + SubPhase context display
- [ ] Assignee picker (TeamMembers of the project only)
- [ ] Due date picker (`react-day-picker`)
- [ ] Tag multi-select
- [ ] Complete toggle
- [ ] Time entries section (linked to M14)
- [ ] Comments thread section (linked to M13)

### Validators

- [ ] `taskSchema` — title required, dates valid, laneId exists in unit
- [ ] `laneSchema` — name required, color valid hex

### Realtime

- [ ] Supabase channel: `unit:{unitId}:tasks` — revalidate Kanban on remote task change

### Tests

- [ ] Unit test: `createTask()` blocks when `maxTasksPerProject` reached
- [ ] Unit test: `moveTask()` — USER cannot move another user's task
- [ ] Unit test: lanes are unit-scoped (BR-21)
- [ ] E2E: drag task from "To Do" to "In Progress" → `moveTask()` called

---

## M13 — Task Comments & @Mentions

### Server Actions (`lib/queries/comments.ts`)

- [ ] `createComment(taskId, authorId, body)`:
  - Parse `@mentions` from body text (regex: `@[username]`)
  - Create `TaskMention` records for each valid mention
  - Validate: mentioned user must be TeamMember or ADMIN/OWNER (NOTIF-12)
  - Cannot `@mention` yourself
  - Send `TASK` notification to each mentioned user: `"[Author] vous a mentionné dans [Task title]"`
  - `unstable_noStore()` — never cache comments
- [ ] `editComment(commentId, body)` — own comments only; set `edited = true`
- [ ] `deleteComment(commentId)` — own comment or ADMIN/OWNER; set `body = "[Message supprimé]"` (soft delete)

### Comment Components

- [ ] `CommentInput` (`components/comments/CommentInput.tsx`):
  - Avatar + textarea
  - `@` trigger → `<Command>` dropdown autocomplete (TeamMembers only)
  - Mention chips rendered inline (indigo color)
  - Submit on `Ctrl+Enter`
- [ ] `CommentsThread` (`components/comments/CommentsThread.tsx`):
  - Chronological list, author avatar + name, timestamp (relative)
  - Edit/delete dropdown menu (own comments or ADMIN/OWNER)
  - Deleted comments render as `"[Message supprimé]"`
  - Mention names highlighted as indigo chips

### Realtime

- [ ] Supabase channel: `task:{taskId}:comments` — live comment append on new comment

### Validators

- [ ] `commentSchema` — body required, max 2000 chars

### Tests

- [ ] Unit test: `createComment()` — mention creates `TaskMention` record + notification
- [ ] Unit test: `createComment()` — self-mention is blocked
- [ ] Unit test: `createComment()` — mention of non-TeamMember is blocked
- [ ] Unit test: `deleteComment()` — sets body to `"[Message supprimé]"` (not hard delete)

---

## M14 — Time Tracking

### Server Actions (`lib/queries/time.ts`)

- [ ] `createTimeEntry(data)`:
  - USER can only log on assigned projects (BR)
  - ADMIN/OWNER can log on any unit project
  - Auto-calculate `duration` in minutes from `startTime` / `endTime`
  - `revalidateTag(CacheTags.timeEntries)`
- [ ] `updateTimeEntry(id, data)` — own entry or ADMIN/OWNER
- [ ] `deleteTimeEntry(id)` — own entry or ADMIN/OWNER

### Time Tracking Tab (inside Project Detail)

- [ ] Entries grouped by user
- [ ] Weekly total per user, grand total
- [ ] Manual entry form: date, `startTime`, `endTime`, description, taskId (optional)
- [ ] `useTimer` hook — live stopwatch on task, auto-fills `startTime`/`endTime` on stop

### Task Detail Sheet — Time Section

- [ ] List of time entries: avatar, duration, description, date

### Validators

- [ ] `timeEntrySchema` — `startTime < endTime`, projectId + userId required

### Tests

- [ ] Unit test: `createTimeEntry()` — USER blocked from logging on non-assigned project
- [ ] Unit test: `duration` is auto-calculated (not user-editable)

---

## M15 — Notifications System

### Server Actions (`lib/queries/notifications.ts`)

- [ ] `createNotification(data)` — internal utility called by all server actions; never called directly from client
- [ ] `getUnreadCount(userId)` — `unstable_noStore()`, returns `Int`
- [ ] `getNotifications(userId, filters?)` — `unstable_noStore()`, returns paginated list
- [ ] `markNotificationRead(notificationId)` — own notifications only
- [ ] `markAllNotificationsRead(userId)` — own notifications only

### NotificationBell Component (`components/notifications/NotificationBell.tsx`)

- [ ] Badge showing `unreadCount` (hides at 0)
- [ ] Polling: `useNotifications()` hook, 30s interval, calls `getUnreadCount()` (NOTIF-08/09/10)
- [ ] Click → dropdown showing latest 5 unread with: type icon, message, relative timestamp
- [ ] "View all" link → `/dashboard/notifications`

### Full Notifications Page (`/dashboard/notifications`)

- [ ] Create page route
- [ ] Filter tabs: All / Unread / by Type (`INVITATION | PROJECT | TASK | PRODUCTION | ...`)
- [ ] "Mark all as read" action button
- [ ] Infinite scroll or pagination

### Notification Triggers Map

Ensure `createNotification()` is called in the following server actions:

- [ ] `inviteMember()` → type: `INVITATION`, recipient: invitee
- [ ] `acceptInvitation()` → type: `INVITATION`, recipient: ADMIN/OWNER
- [ ] `createProject()` → type: `PROJECT`, recipients: OWNER + ADMIN
- [ ] `updateProject(status)` → type: `PROJECT`, recipients: OWNER + ADMIN + TeamMembers
- [ ] `createTask(assignedUserId)` → type: `TASK`, recipient: assigned user
- [ ] `addTeamMember()` → type: `TEAM`, recipient: added user
- [ ] `removeTeamMember()` → type: `TEAM`, recipient: removed user
- [ ] `updatePhase(status)` → type: `PHASE`, recipients: OWNER + ADMIN
- [ ] `createProduction()` (variance) → type: `PRODUCTION`, recipient: OWNER (BR-14)
- [ ] `createLane()` → type: `LANE`, recipients: OWNER + ADMIN
- [ ] `createComment()` (mention) → type: `TASK`, recipient: mentioned user

### Realtime (bell badge)

- [ ] Supabase channel: `unit:{unitId}:notifications` — push to bell badge on new notification

### Tests

- [ ] Unit test: `getUnreadCount()` uses `unstable_noStore()`
- [ ] Unit test: `markAllNotificationsRead()` only affects own notifications
- [ ] Unit test: production variance triggers `PRODUCTION` notification to OWNER (BR-14)

---

## M16 — Document Management

### Server Actions (`lib/queries/documents.ts`)

- [ ] `createDocument(projectId, file)` — Uploadthing upload; store URL, name, type, size, uploaderId
- [ ] `deleteDocument(documentId)` — delete from DB + Uploadthing (ADMIN/OWNER only)

### Documents Tab (inside Project Detail)

- [ ] File list table: name, type icon, size (formatted), uploader avatar + name, upload date
- [ ] Upload button (ADMIN/OWNER only) — Uploadthing dropzone (PDF, PNG, JPG, DWG — max 10 MB)
- [ ] Download link (all TeamMembers)
- [ ] Delete with confirmation (ADMIN/OWNER only)

### RBAC

- [ ] TeamMembers (USER): view + download only
- [ ] ADMIN/OWNER: upload + delete

### Tests

- [ ] Unit test: `deleteDocument()` — USER cannot delete
- [ ] Unit test: file type/size validation before upload

---

## M17 — Activity Logs

### Server Actions (`lib/queries/activity.ts`)

- [ ] `createActivityLog(data)` — internal utility; `unstable_noStore()`; called by all mutating server actions
- [ ] `getActivityLogs(companyId, filters?)` — `unstable_noStore()`; scoped by role:
  - OWNER: all logs company-wide
  - ADMIN: unit-scoped logs
  - USER: project-scoped logs (only their assigned projects)

### Activity Log Triggers

Ensure `createActivityLog()` is called after:

- [ ] Project create / update / delete
- [ ] Phase create / update / delete
- [ ] Task create / update / delete
- [ ] Client create / update / delete
- [ ] Team member add / remove
- [ ] Subscription status change

### Activity Log UI

- [ ] Activity feed component: icon per entityType, action label, user avatar + name, timestamp
- [ ] Filters: date range picker, entityType dropdown, user dropdown
- [ ] Accessible from Unit Dashboard (M19) and Company Dashboard (M05)

### Tests

- [ ] Unit test: `getActivityLogs()` — USER cannot see logs outside assigned projects
- [ ] Unit test: `createActivityLog()` uses `unstable_noStore()`

---

## M18 — User Workspace (USER Role)

### Routes

- [ ] Create `app/(dashboard)/user/[userId]/page.tsx` — Personal dashboard
- [ ] Create `app/(dashboard)/user/[userId]/profile/page.tsx` — Account settings
- [ ] Create `app/(dashboard)/user/[userId]/tasks/page.tsx` — All assigned tasks
- [ ] Create `app/(dashboard)/user/[userId]/projects/page.tsx` — TeamMember project list
- [ ] Create `app/(dashboard)/user/[userId]/analytics/page.tsx` — Personal stats

### Personal Dashboard

- [ ] KPI cards: tasks assigned, tasks due this week, hours logged this week, projects active
- [ ] Upcoming due dates list (next 7 days, sorted by dueDate)
- [ ] Recent notifications (latest 5)

### Personal Analytics

- [ ] Tasks completed this month (count + chart)
- [ ] Time logged per project (bar chart)
- [ ] Projects participated in (list)

### Tests

- [ ] Unit test: USER cannot access another user's workspace (`/user/[otherId]`)

---

## M19 — Unit Dashboard (ADMIN)

### Route

- [ ] Create `app/(dashboard)/unite/[unitId]/page.tsx`

### Unit Dashboard Content

- [ ] KPI cards: active projects, total tasks, open tasks, members, production health %
- [ ] Project health grid (mini-card per project: name, progress bar, status badge)
- [ ] Recent activity feed (last 10 log entries — from M17)
- [ ] Quick action buttons: New Project, Invite Member, New Task

### Tests

- [ ] Unit test: ADMIN can only see their own unit's data

---

## M20 — Public Landing Page

### Route

- [ ] Create `app/page.tsx` (replace Next.js boilerplate)

### Sections

- [ ] Hero: headline, subheadline, CTA → `/company/sign-up`
- [ ] Problem statement: 3–4 pain point cards (from PRD §2)
- [ ] Features overview: Gantt, Kanban, Production Monitoring, Multi-Tenant, RBAC
- [ ] Plan comparison table: Starter / Pro / Premium with feature matrix
- [ ] Testimonials / social proof placeholder
- [ ] Footer: links, company info

### SEO

- [ ] `<title>`, `<meta name="description">` via Next.js `generateMetadata`
- [ ] Semantic HTML (`<main>`, `<section>`, `<article>`)
- [ ] OG image (`/opengraph-image.png`)

### Tests

- [ ] E2E: CTA button navigates to sign-up page
- [ ] Lighthouse score ≥ 90 for Performance, Accessibility, SEO

---

## M21 — Polish & Quality

### Responsiveness

- [ ] Audit all pages at ≥ 768px breakpoint (tablet minimum per PRD)
- [ ] Sidebar collapses to icon-only on < 1024px
- [ ] Tables scroll horizontally on < 900px

### Accessibility

- [ ] WCAG 2.1 AA pass: color contrast, focus rings, ARIA labels
- [ ] Full keyboard navigation (Tab, Shift+Tab, Enter, Escape, Arrow keys)
- [ ] Screen reader pass on: forms, modals, Kanban board

### Error Handling

- [ ] `ErrorBoundary` on every async server component section
- [ ] Global error page (`app/error.tsx`)
- [ ] Not found page (`app/not-found.tsx`)
- [ ] Form-level error states (Zod + react-hook-form)

### Loading States

- [ ] `Skeleton` loaders on all data-fetching components
- [ ] `loading.tsx` files for all route segments
- [ ] Suspense boundaries around Gantt and Kanban

### Empty States

- [ ] Empty state for: Projects list, Clients list, Kanban (no tasks), Notifications, Activity Logs
- [ ] Each empty state: illustration/icon + message + CTA button

### Performance Targets

- [ ] LCP < 2.5s (NFR-01)
- [ ] Server Actions respond < 500ms (NFR-02)
- [ ] Kanban: 200 tasks across 10 lanes without degradation (NFR-04)
- [ ] Gantt: 50 phases without visible lag (NFR-03)

### Rate Limiting

- [ ] Apply rate limits on: `createComment()`, `createTimeEntry()`, notification polling

### Realtime Resilience

- [ ] Supabase disconnection handling: show reconnect indicator, re-fetch on reconnect

---

## M22 — Caching & Performance

### Cache Directives

- [ ] Add `'use cache'` to all cacheable server functions (not: notifications, comments, activity logs)
- [ ] Apply `cacheTag()` to all cached functions using tags from `lib/cache.ts`
- [ ] Apply `cacheLife()` profiles:
  - `"static"`: plans, wilayas, company header
  - `"days"`: company, unit settings
  - `"hours"`: project lists, phase lists, client lists

### Cache Invalidation

- [ ] Every mutating server action calls `revalidateTag()` with the correct entity tag
- [ ] Invalidation map verified — no over-broad invalidation
- [ ] `revalidatePath()` is **never used** — only `revalidateTag()`
- [ ] `unstable_noStore()` used for: `getNotifications()`, `getUnreadCount()`, `getActivityLogs()`, `getComments()`

### Verification Checklist

- [ ] No `any` type anywhere in codebase (`pnpm typecheck` must pass clean)
- [ ] No direct `fetch()` calls to internal API routes — use Server Actions only
- [ ] `companyId` present in every Prisma query that touches user data (BR-01)
- [ ] All new patterns documented in `AGENTS.md`

---

_End of Task Tracker v3.0.0 — PMA Fresh Start_
